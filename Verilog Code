module johnson(input clk,reset,input [3:0] load,output reg [3:0] q);
  always@(posedge clk)
    begin
      if(reset)
        q<=load;
      else
        q<={~q[0],q[3:1]};
    end
endmodule
        
        
        
